# Wedding Less 4.6.7 Refresh

## Status: Completed

## Context

The site compiles its tracked Less source during development and CI, never in
the browser. The manifest exactly pinned 4.6.4 while npm published 4.6.7 with
parser and debug-output fixes plus a refreshed optional directory helper.

## Requirements

- Update only the exact build-time compiler pin and generated lock graph.
- Preserve JavaScript-disabled compilation and byte-identical tracked CSS.
- Pin the reviewed npm registry integrity rather than introducing a version
  range or unreviewed source.
- Keep runtime dependencies, browser assets, routes, headers, templates,
  deployment status, and application behavior unchanged.

## Verification Plan

- Run the Node test suite and deterministic CSS build before and after update.
- Require the manifest, lock version, registry integrity, and expected
  `make-dir` transitive version in the dependency-free checker.
- Mutate the reviewed lock integrity in an isolated checkout and require
  `make check` to fail closed.
- Run `npm audit --prefix app`, `npm outdated --prefix app`, and the full gate
  on supported Node.js 20, 22, and 24.

## Work Completed

- Updated `less` from 4.6.4 to the exact 4.6.7 release.
- Locked the reviewed SHA-512 registry integrity and `make-dir` 5.1.0.
- Removed the no-longer-resolved optional `pify` and `semver` packages.
- Preserved the tracked CSS artifact byte-for-byte.

## Verification

- Baseline and updated Node suites passed 27 tests.
- The updated build produced no `app/public/css/main.css` diff.
- `npm audit --prefix app` reported zero vulnerabilities and
  `npm outdated --prefix app` returned no packages.
- A hostile mutation of the Less tarball integrity failed on the intended
  checker assertion.
- Disposable Node 20, 22, and 24 containers each passed `make check`, including
  27 tests, deterministic CSS generation, static contracts, and all 30 Make
  authority cases.
- `git diff --check` passed; exact-head review and hosted evidence remain the
  final pre-merge actions.
- Hosted Node 20, 22, and 24 verification plus CodeQL passed on PR #17.
- The Codex review helper returned HTTP 401 before analysis; an immutable
  manual review confirmed the local and PR heads matched and found no
  actionable issue.
